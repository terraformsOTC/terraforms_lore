# deps
node_modules/

# Next.js
.next/
out/

# Vercel
.vercel/

# Local env (contains KV credentials — never commit)
.env
.env.local
.env*.local

# Local submissions fallback (prod uses KV)
submissions.json

# OS
.DS_Store

# Syncthing machine-local files (not synced between devices)
.stfolder/
.stignore

# Claude Code per-machine permission allowlist (local only)
.claude/settings.local.json
